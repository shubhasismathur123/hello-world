apiVersion: v1
kind: Service
metadata:
  name: myworkflow
  namespace: namespace-workflow-1729571124245
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: myworkflow
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000